<tr class="border-b dark:border-stone-700 hover:bg-stone-200 dark:hover:bg-stone-700<%= highlighted ? ' animate-highlight' : '' %>" id="<%= dom_id(currency, :exportable) %>">
    <th scope="row" class="px-4 py-3 w-4">
        <div class="flex items-center mr-3">
            <%= image_tag currency.image(16), class: 'h-8 w-auto mr-3', data: { lazy_src: currency.image(64) } %>
            <%= currency %>
        </div>
    </th>
    <td class="px-4 py-3 text-center"><%= currency.ticker.upcase %></td>
    <td class="px-4 py-3"><%= number_to_currency(currency.price) %></td>
    <td class="px-4 py-3"><%= currency.updated_at %></td>
</tr>
