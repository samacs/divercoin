<header>
    <nav class="bg-white border-stone-200 px-4 lg:px-6 py-2.5 dark:bg-stone-800 drop-shadow-md">
        <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl">
            <%= render_layout_partial('logo') %>
            <%= render_header_partial('user_menu') %>
            <% if signed_in? %>
            <% else %>
                <%= render_header_partial('top_menu') %>
            <% end %>
        </div>
    </nav>
</header>
<% if signed_in? %>
    <%= render_header_partial('dashboard_menu') %>
<% end %>
