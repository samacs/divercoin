<header>
    <nav class="bg-white absolute left-0 top-0 w-full mx-auto border-stone-200 px-4 lg:px-6 py-2.5 dark:bg-stone-800 drop-shadow-md">
        <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl">
            <%= render_layout_partial('logo') %>
            <%= render_header_partial('user_menu') %>
            <%= render_header_partial('top_menu') %>
        </div>
    </nav>
</header>
